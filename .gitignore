# Web artifacts
*.html

Wait, I need to reconsider this. HTML files are typically source code files, not build artifacts. Based on rule #7, I should not ignore source code files unless they are clearly generated or sensitive. An HTML file like 'pdf_viewer.html' is likely a source file that should be tracked in version control.

Since only source/config files were added (HTML file), and no build artifacts, dependencies, or temp files were included in the changes, the output should be empty.

(pdf_viewer.html is a source file, not a build artifact)